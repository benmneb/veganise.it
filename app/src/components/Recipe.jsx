// import { useParams } from 'react-router-dom';

import { DialogTitle, DialogContent } from '@material-ui/core';

export default function Recipe() {
	// const { id } = useParams();

	return (
		<section>
			<DialogTitle>Delicious</DialogTitle>
			<DialogContent>yummy recipes</DialogContent>
		</section>
	);
}
